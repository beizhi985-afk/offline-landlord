# 更新日志

## 阶段2 - 公共房间与 LAN 网络基础设施边界

### 架构

- 新增与游戏无关的 `network.transport`，包含 LAN 端点、TCP客户端、TCP服务端、连接标识和 UDP datagram 收发。
- `LanGameClient` 保持原有 API，继续负责斗地主 V4 JOIN/ACTION/STATE、状态Flow、resumeToken和重连，只把原始Socket读写交给公共transport。
- `LanGameServer` 继续负责斗地主 V4 解析、玩家与连接映射、revision转发和错误响应，只把监听、连接生命周期及原始字符串收发交给公共transport。
- UDP datagram I/O与 `DDZ_DISCOVER_V4` / `DDZ_HOST_V4` 文本codec分离；线上请求、响应和字段顺序保持不变。

### 兼容与测试

- 网络协议仍为V4，没有增加`gameType`，没有改变WireType、WireEnvelope字段、房间码或断线8秒托管行为。
- 新增5项纯TCP transport测试、1项纯UDP transport测试、6项V4 golden兼容测试、3项UDP discovery codec测试和1项真实TCP重连映射测试。
- 原49项测试继续通过，连同阶段2新增16项共65/65项通过，0失败、0错误、0跳过。
- 现有机器人压力入口完成2000/2000局；Android debug构建成功。
- 没有修改GameEngine、GameModels、BotBrain、规则、计分、12/24局或加倍逻辑。
- 没有开发UNO网络，没有引入第三方网络库，没有升级targetSdk或增加局域网权限。
- 阶段2已完成自动测试、本机真实TCP回环测试及2000局机器人压力测试；本轮未重新执行三台Android真机热点验收，该风险已知并接受。

## 阶段1 - 多游戏入口与工程边界

### 新增

- 新增“离线牌局”游戏选择页，提供斗地主和UNO两个入口。
- 斗地主入口继续组合原有 `OfflineLandlordApp`，没有复制或重写斗地主页面。
- UNO入口仅进入“开发中”占位页，可返回游戏选择，不包含任何游戏逻辑或联网能力。
- 新增轻量 `GameType`、`AppRoute` 和纯状态导航，不引入Navigation Compose或其他UI框架。
- 新增5项导航状态测试，覆盖默认入口、斗地主、UNO占位、返回选择和游戏类型集合。
- 原斗地主44项测试全部通过，连同阶段1新增测试共49/49项通过；Android debug构建成功。

### 边界与兼容

- 斗地主ViewModel只在进入斗地主路由后创建；房间、TCP客户端和UDP发现仍按原有用户操作启动。
- 未移动斗地主核心、牌桌、机器人或网络文件，未改变规则、计分、托管与断线恢复。
- 应用显示名称改为“离线牌局”；`applicationId`仍为`com.offlinelandlord.game.v2`，网络协议仍为V4。
- 公共房间与网络边界梳理留待阶段2处理。

## v0.3.7 - 牌局回合提示优化版

### 优化

- 叫地主、加倍和出牌阶段的中央状态改为根据当前回合实时生成。
- 牌局进行中不再显示玩家断线、重新连接或机器人接管等连接事件。
- 出牌提示同时显示当前玩家昵称及“地主/农民”身份，避免两个农民之间产生歧义。
- 等待房间与整场结算仍保留原有状态信息。

### 测试

- 新增牌局中断线、重连不覆盖回合提示的回归测试。
- 新增地主出牌后切换到农民方的提示测试。
- 冻结前强制重新执行全部测试，44/44 项通过，0失败、0错误、0跳过。
- Android V3.7 调试 APK 构建和签名验证成功。
- 版本号升级为 `0.3.7`（`versionCode 10`），网络协议仍为V4。

### 基线冻结

- V3.7 基线冻结状态：已批准。
- 基线版本为 `versionName 0.3.7`、`versionCode 10`、`applicationId com.offlinelandlord.game.v2`、网络协议V4。
- 本次 `v3.7-baseline` 冻结未重新完成完整三台真机验收。项目负责人决定接受该剩余风险，以当前自动测试、APK构建验证和已有开发验证结果作为后续开发的回退基线。
- 三机验收未标记为通过，后续仍可按照 `docs/release/v3.7-device-test.md` 补充真实验收记录。

## v0.3.6 - 联机动作发送修复版

### 修复

- 修复加入玩家点击“准备好啦”时显示发送失败的问题。
- 客户端此前在Compose界面主线程同步执行Socket写入和刷新，Android会阻止这种网络I/O。
- `LanGameClient.sendAction` 改为挂起函数并固定在 `Dispatchers.IO` 执行。
- `GameViewModel`统一通过协程处理房主和加入玩家动作，覆盖准备、叫分、加倍、出牌、不出和托管。

### 测试

- TCP集成测试扩展为完整的“客户端加入→发送准备动作→服务端收到并更新玩家状态”流程。
- 连同端口、手势、规则、机器人、整场模拟和协议测试共42项全部通过。
- 版本号升级为 `0.3.6`（`versionCode 9`），网络协议仍为V4。

## v0.3.5 - 局域网联机修复版

### 修复

- 修复 `Socket.apply` 内部的 `port` 属性遮蔽：旧版错误读取新 Socket 的初始端口0，而不是玩家输入或自动发现得到的服务端端口。
- 客户端现在明确使用 `LanGameClient.host` 与 `LanGameClient.port` 建立 TCP 连接。
- Socket 在开始连接前即纳入关闭管理，连接异常时不会遗留未关闭的本地 Socket。

### 防错与提示

- 连接前验证房主 IP、1～65535端口范围和六位数字房间码。
- 手动加入的 IP 输入框不再预填 `192.168.43.1`，并提示不要包含冒号和端口。
- 连接超时、拒绝连接和 IP 格式错误改为中文提示，不再直接显示安卓底层英文异常。
- 版本号升级为 `0.3.5`（`versionCode 8`），网络协议仍为 V4。

### 测试

- 新增真实回环 TCP 集成测试，启动随机非零端口服务端并验证客户端成功连接和收到房间状态。
- 新增端口0连接前拒绝测试；连同现有测试共41项全部通过。

## v0.3.4 - 手牌滑动多选版

### 新增

- 保留原有单张点击选牌，新增整块手牌区域的横向滑动批量选择。
- 从未选中的牌开始滑动时批量选中，从已选中的牌开始时批量取消；单次手势模式固定。
- 支持左右双向滑动、越界钳制以及滑出后反向缩小临时范围。
- 滑动过程中以抬起和薄荷色描边实时预览，松手后才写入正式选牌集合。

### 交互与兼容

- 使用 Android/Compose 的系统触摸阈值区分点击与横向拖动，拖动消费事件后不会额外触发点击。
- 根据重叠间距而不是牌宽计算手指所在牌，最后一张牌和左右边界均单独处理。
- 选牌继续使用唯一 `card.id`，出牌仍进入原有牌型判断，不修改 AI、积分、网络协议和整场赛制。
- 版本号升级为 `0.3.4`（`versionCode 7`），与 v0.3.3 使用相同通信协议。

### 测试

- 新增 17 项有效手势状态机测试，覆盖点击、双向拖动、批量取消、回滑缩短、越界、重叠坐标、唯一 ID、阈值和取消清理。
- 连同现有规则、机器人、整场模拟和通信测试共 39 项自动化测试全部通过。

## v0.3.3 - 更新提示与整场战绩版

### 新增

- 安装新 APK 后首次打开会显示一次本地更新说明，首页“更新”按钮可随时重新查看。
- 更新提示只读取 APK 内置内容和本机版本号，不访问 GitHub，也不需要互联网。
- 每次发牌都会从三名玩家中随机选择第一位叫地主的玩家，并在牌桌中央明确提示。
- 每局结束时记录首叫玩家、地主、获胜方、倍数、春天、三人的分数变化和累计分。
- 12 局或 24 局全部完成后显示整场排行榜，按总分和胜局数排序。
- 总结页可滚动查看每一局的输赢和三名玩家当局加减分、累计分。

### 通信与版本

- 完整战绩由房主同步给同一房间的所有手机，三人看到相同的总结数据。
- 局域网通信协议升级为 V4，参加同一房间的手机需全部安装 v0.3.3。
- 版本号升级为 `0.3.3`（`versionCode 6`），可覆盖升级现有 V2/V3。

## v0.3.2 - 局数与加倍版

### 新增

- 创建房间时可选择 12 局或 24 局，牌桌顶部持续显示当前局数进度。
- 创建房间时可选择是否允许加倍；开启后，地主确定后由三名玩家依次选择“加倍”或“不加倍”。
- 每名玩家选择加倍都会使当前倍数翻倍，加倍结果同步显示在三台手机上。
- 局数完成后停止继续准备，并显示整场完成和返回首页操作。

### 修复

- “不出”和“出牌”移动到手牌上方，避免遮住手牌。
- 操作按钮改为按需出现：轮不到自己时全部隐藏，选中有效手牌后才出现“出牌”，有上一手牌可跟时才出现“不出”。
- 切换回合或不出后自动清空选牌，避免保留上一回合的错误选中状态。

### 通信与版本

- 房间发现信息新增局数和加倍设置，等待房间会明确展示本场模式。
- 局域网通信协议升级为 V3，参加同一房间的手机需全部安装 v0.3.2。
- 版本号升级为 `0.3.2`（`versionCode 5`），可覆盖升级现有 V2/V3。

## v0.3.1 - 牌桌布局修订

### 修改

- 三张地主底牌固定在屏幕顶部中央，不再占用中央出牌区。
- “不出”和“出牌”整体上移，并降低到 80% 透明度，和牌区重叠时仍能看清下层内容。
- 上一手牌根据出牌者分别靠近左侧、右侧或下方玩家显示，长牌型自动收紧间距。
- 地主底牌及桌面小牌中的大小王改为红色、黑色竖排 `JOKER`。
- 有实际出牌时隐藏重复的中央“某某出了某牌型”提示；“不出”等回合状态仍正常显示。

### 版本

- 版本号升级为 `0.3.1`（`versionCode 4`），继续沿用 V2/V3 应用 ID，可直接覆盖升级。

## v0.3.0 - 清新界面版

### 新增

- 大厅、等待房间和牌桌统一升级为奶油白、雾蓝、蜜桃粉、薄荷绿和淡紫的清新 INS 风格。
- 全新原创圆角纸牌，强化点数、花色、红黑配色和重叠手牌的可读性。
- 牌桌改为上方状态、中间对手与出牌区、下方扇形手牌的横屏三段布局。
- 新增原创风景背景、玩家头像、机器人标识、剩余牌堆和当前回合高亮。
- 新增原创手绘半写实湖畔风景，并作为本地图片随 APK 打包。
- 除本地背景外，其余头像、纸牌和控件均由 Compose 绘制，不使用远程图片、在线字体或云端资源。

### 修改

- 从本版本开始沿用 V2 应用 ID，通过版本号覆盖升级。
- 版本号升级为 `0.3.0`（`versionCode 3`）。
- 缩小牌桌人物头像，移除中央椭圆牌桌和机器人背后的大面积白色底板，让湖畔背景直接呈现。
- 移除首页、房间、牌桌和结算中的随机氛围短句，只保留必要游戏信息。
- 结算改为不透底的居中浮层，在 854×480 横屏下完整显示胜负、倍数和下一局操作。

### 验收

- 增加 V3 界面截图验收清单，覆盖大厅、等待房间、叫分和正式出牌画面。

## v0.2.0 - 机器人版

### 新增

- 房主可在等待房间中添加或移除机器人。
- 支持一名真人加两个机器人、两名真人加一个机器人。
- 支持玩家主动托管和取消托管。
- 玩家断线 8 秒后自动由机器人代打，重连后自动取回控制权。
- 新增完整合法出牌生成器和离线启发式机器人策略。
- V2 使用独立应用 ID，可与 V1 同时安装。

### 测试

- 新增合法出牌生成、机器人补位、托管和重连测试。
- 完成 2000 局机器人自动对战压力测试，无非法操作和死局。

## v0.1.0 - 首个可玩版本

### 新增

- 经典三人斗地主规则、叫分、出牌、炸弹、春天和结算。
- 热点局域网自动发现、手动 IP 加入和六位房间码。
- TCP 状态同步、基础断线重连和横屏 Compose 牌桌。
- 首个可侧载安装的 Android 调试 APK。

## 后续版本规则

从 V2 之后沿用同一个应用 ID，不再为每个版本复制独立工程。每次发布只需要：

1. 递增 `versionCode` 和 `versionName`。
2. 更新本文件，记录新增、修改、修复和已知问题。
3. 生成带版本号的 APK。
4. 创建对应 Git 标签，保留历史版本源码。

这样后续 APK 可以直接覆盖升级，同时仍可通过 Git 标签查看或恢复任意历史版本。
