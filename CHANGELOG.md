# 更新日志

## v0.3.3 - 更新提示与整场战绩版

### 新增

- 安装新 APK 后首次打开会显示一次本地更新说明，首页“更新”按钮可随时重新查看。
- 更新提示只读取 APK 内置内容和本机版本号，不访问 GitHub，也不需要互联网。
- 每次发牌都会从三名玩家中随机选择第一位叫地主的玩家，并在牌桌中央明确提示。
- 每局结束时记录首叫玩家、地主、获胜方、倍数、春天、三人的分数变化和累计分。
- 12 局或 24 局全部完成后显示整场排行榜，按总分和胜局数排序。
- 总结页可滚动查看每一局的输赢和三名玩家当局加减分、累计分。

### 通信与版本

- 完整战绩由房主同步给同一房间的所有手机，三人看到相同的总结数据。
- 局域网通信协议升级为 V4，参加同一房间的手机需全部安装 v0.3.3。
- 版本号升级为 `0.3.3`（`versionCode 6`），可覆盖升级现有 V2/V3。

## v0.3.2 - 局数与加倍版

### 新增

- 创建房间时可选择 12 局或 24 局，牌桌顶部持续显示当前局数进度。
- 创建房间时可选择是否允许加倍；开启后，地主确定后由三名玩家依次选择“加倍”或“不加倍”。
- 每名玩家选择加倍都会使当前倍数翻倍，加倍结果同步显示在三台手机上。
- 局数完成后停止继续准备，并显示整场完成和返回首页操作。

### 修复

- “不出”和“出牌”移动到手牌上方，避免遮住手牌。
- 操作按钮改为按需出现：轮不到自己时全部隐藏，选中有效手牌后才出现“出牌”，有上一手牌可跟时才出现“不出”。
- 切换回合或不出后自动清空选牌，避免保留上一回合的错误选中状态。

### 通信与版本

- 房间发现信息新增局数和加倍设置，等待房间会明确展示本场模式。
- 局域网通信协议升级为 V3，参加同一房间的手机需全部安装 v0.3.2。
- 版本号升级为 `0.3.2`（`versionCode 5`），可覆盖升级现有 V2/V3。

## v0.3.1 - 牌桌布局修订

### 修改

- 三张地主底牌固定在屏幕顶部中央，不再占用中央出牌区。
- “不出”和“出牌”整体上移，并降低到 80% 透明度，和牌区重叠时仍能看清下层内容。
- 上一手牌根据出牌者分别靠近左侧、右侧或下方玩家显示，长牌型自动收紧间距。
- 地主底牌及桌面小牌中的大小王改为红色、黑色竖排 `JOKER`。
- 有实际出牌时隐藏重复的中央“某某出了某牌型”提示；“不出”等回合状态仍正常显示。

### 版本

- 版本号升级为 `0.3.1`（`versionCode 4`），继续沿用 V2/V3 应用 ID，可直接覆盖升级。

## v0.3.0 - 清新界面版

### 新增

- 大厅、等待房间和牌桌统一升级为奶油白、雾蓝、蜜桃粉、薄荷绿和淡紫的清新 INS 风格。
- 全新原创圆角纸牌，强化点数、花色、红黑配色和重叠手牌的可读性。
- 牌桌改为上方状态、中间对手与出牌区、下方扇形手牌的横屏三段布局。
- 新增原创风景背景、玩家头像、机器人标识、剩余牌堆和当前回合高亮。
- 新增原创手绘半写实湖畔风景，并作为本地图片随 APK 打包。
- 除本地背景外，其余头像、纸牌和控件均由 Compose 绘制，不使用远程图片、在线字体或云端资源。

### 修改

- 从本版本开始沿用 V2 应用 ID，通过版本号覆盖升级。
- 版本号升级为 `0.3.0`（`versionCode 3`）。
- 缩小牌桌人物头像，移除中央椭圆牌桌和机器人背后的大面积白色底板，让湖畔背景直接呈现。
- 移除首页、房间、牌桌和结算中的随机氛围短句，只保留必要游戏信息。
- 结算改为不透底的居中浮层，在 854×480 横屏下完整显示胜负、倍数和下一局操作。

### 验收

- 增加 V3 界面截图验收清单，覆盖大厅、等待房间、叫分和正式出牌画面。

## v0.2.0 - 机器人版

### 新增

- 房主可在等待房间中添加或移除机器人。
- 支持一名真人加两个机器人、两名真人加一个机器人。
- 支持玩家主动托管和取消托管。
- 玩家断线 8 秒后自动由机器人代打，重连后自动取回控制权。
- 新增完整合法出牌生成器和离线启发式机器人策略。
- V2 使用独立应用 ID，可与 V1 同时安装。

### 测试

- 新增合法出牌生成、机器人补位、托管和重连测试。
- 完成 2000 局机器人自动对战压力测试，无非法操作和死局。

## v0.1.0 - 首个可玩版本

### 新增

- 经典三人斗地主规则、叫分、出牌、炸弹、春天和结算。
- 热点局域网自动发现、手动 IP 加入和六位房间码。
- TCP 状态同步、基础断线重连和横屏 Compose 牌桌。
- 首个可侧载安装的 Android 调试 APK。

## 后续版本规则

从 V2 之后沿用同一个应用 ID，不再为每个版本复制独立工程。每次发布只需要：

1. 递增 `versionCode` 和 `versionName`。
2. 更新本文件，记录新增、修改、修复和已知问题。
3. 生成带版本号的 APK。
4. 创建对应 Git 标签，保留历史版本源码。

这样后续 APK 可以直接覆盖升级，同时仍可通过 Git 标签查看或恢复任意历史版本。
